{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}MeetUp{% endblock %}

{% block main %}
<div class="container-fluid offset-1 col-10">

    <h1 class="mb-3">MeetUp</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meet_up.id }}</td>
            </tr>
            <tr>
                <th>Employée</th>
                <td>{{ meet_up.getEmployee }}</td>
            </tr>
             <tr>
                <th>Auteur</th>
                <td>{{ meet_up.getAuthor }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ meet_up.date ? meet_up.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meet_up_index') }}">back to list</a>

    <a href="{{ path('meet_up_edit', {'id': meet_up.id}) }}">edit</a>

    {{ include('meet_up/_delete_form.html.twig') }}
</div>

{% endblock %}
