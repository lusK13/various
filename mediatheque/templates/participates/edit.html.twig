{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Edit Participates{% endblock %}

{% block main %}
<div class="container mt-3">
    <h1>Edit Participates</h1>

    {{ include('participates/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('participates_index') }}">back to list</a>

    {{ include('participates/_delete_form.html.twig') }}
</div>
{% endblock %}
