{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}IsInvolvedIn{% endblock %}

{% block main %}
<div class="container-fluid offset-1 col-10">

    <h1 class="mb-3">IsInvolvedIn</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ is_involved_in.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ is_involved_in.role }}</td>
            </tr>
            <tr>
                <th>Document</th>
                <td>{{ is_involved_in.getDocument }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ is_involved_in.getAuthor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('is_involved_in_index') }}">back to list</a>

    <a href="{{ path('is_involved_in_edit', {'id': is_involved_in.id}) }}">edit</a>

    {{ include('is_involved_in/_delete_form.html.twig') }}
</div>

{% endblock %}
