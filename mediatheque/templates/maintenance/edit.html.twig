{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Edit Maintenance{% endblock %}

{% block main %}
<div class="container mt-3">

    <h1>Edit Maintenance</h1>

    {{ include('maintenance/_form.html.twig', {'button_label': 'Update'}) }}

    <button type="button" class="btn btn-secondary mb-3"><a href="{{ path('maintenance_index') }}">back to list</a></button>

    {{ include('maintenance/_delete_form.html.twig') }}
</div>

{% endblock %}
